# =============================
# Environment Example for n8n + FastAPI + ngrok
# Copy to .env and adjust values
# =============================

# ---- Core ngrok / Domain ----
# Subdomain you reserved in ngrok dashboard (exclude base domain)
SUBDOMAIN=your-subdomain
# Base domain (free tier is usually ngrok-free.app)
DOMAIN_NAME=ngrok-free.app
# ngrok auth token (Get from https://dashboard.ngrok.com/get-started/your-authtoken)
N8N_AUTHTOKEN=REPLACE_WITH_NGROK_TOKEN

# ---- n8n Runtime ----
GENERIC_TIMEZONE=Europe/Amsterdam
# Set to false when only local dev (cookies on localhost); true when remote/public
N8N_SECURE_COOKIE=false
# Uncomment and set in production to encrypt credentials at rest
# N8N_ENCRYPTION_KEY=CHANGE_ME_TO_LONG_RANDOM_VALUE
# N8N_USER_MANAGEMENT_JWT_SECRET=CHANGE_ME_JWT_SECRET

# Host/URL overrides (usually not needed when using ngrok)
# N8N_HOST=n8n.example.com
# N8N_PROTOCOL=https
# WEBHOOK_URL=https://n8n.example.com/
# NODE_ENV=production

# ---- Optional Task Runners / Future Config ----
# N8N_RUNNERS_ENABLED=true

# ---- Database (Using SQLite by default) ----
# For PostgreSQL switch, you would add:
# DB_TYPE=postgresdb
# DB_POSTGRESDB_HOST=postgres
# DB_POSTGRESDB_PORT=5432
# DB_POSTGRESDB_DATABASE=n8n
# DB_POSTGRESDB_USER=n8n
# DB_POSTGRESDB_PASSWORD=changeme123

# ---- Misc Security & Behavior ----
# N8N_BLOCK_ENV_ACCESS_IN_NODE=true
# N8N_GIT_NODE_DISABLE_BARE_REPOS=true

# End of file
